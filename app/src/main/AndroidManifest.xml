<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clases.fundamentales">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LiveCycleTest"
            android:label="Live Cycle Test" />
        <activity android:name=".SingleTouchTest"
            android:label="SingleTouchTestt" />

    <activity android:name=".MultiTouchTest"
        android:label="MultiTouchTest" />

        <activity android:name=".KeyTest"
            android:label="KeyTest" />
        <activity android:name=".AcelerometerTest"
        android:label="KeyTest" />
        <activity android:name=".AssestsTest"
            android:label="AssestsTest" />
</application>

</manifest>